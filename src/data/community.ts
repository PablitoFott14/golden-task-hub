/**
 * Where a contributor asks for feedback on a task.
 *
 * It is a thread on the Outlier community, not anything the hub runs, so the
 * button leaves the site and is marked as leaving it. If the thread is ever
 * replaced, this is the only line that changes.
 */
export const feedbackThread = {
  url: "https://community.outlier.ai/t/want-feedback-on-a-task-drop-it-here/2964071",
  cta: "Want feedback on a task?",
};
